name: trko
description: A sample app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  scoped_model: ^1.1.0
  flutter_staggered_animations: "^0.1.1"
  http: ^0.12.2
  fluttertoast: ^8.0.7
  flutter_spinkit: "^4.1.0"
  

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true


  assets:
    - assets/images/
    - assets/icons/


  # fonts:
  #   - family: Hz
  #     fonts:
  #       - asset: assets/fonts/HzRegular.otf
 